{
  "name": "clerkai",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.96.0"
  }
}
