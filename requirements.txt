# ── Web Framework ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
python-multipart>=0.0.12

# ── AI / ML ────────────────────────────────────────────────────
openai>=1.51.0          # Whisper STT (whisper-1 모델)
groq>=0.11.0            # Groq API — Llama 3.1 8B & 3.3 70B 요약
pyannote.audio>=3.3.0   # 화자 분리 (HuggingFace token 필요)
torch>=2.0.0            # pyannote 의존성
torchaudio>=2.0.0       # 오디오 처리

# ── Audio Processing ───────────────────────────────────────────
pydub>=0.25.1           # 오디오 포맷 변환 (m4a/webm → wav)

# ── Utils ──────────────────────────────────────────────────────
python-dotenv>=1.0.1    # .env 환경변수 로드
aiofiles>=24.1.0        # 비동기 파일 처리
